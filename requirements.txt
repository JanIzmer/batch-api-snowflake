# Runtime dependencies, pinned for reproducible container builds.
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
tenacity==9.1.4
pyarrow==17.0.0
snowflake-connector-python==3.12.2
structlog==24.4.0
typer==0.27.2
# click is typer's transitive dependency and is pinned deliberately: click
# 8.2 tightened flag validation in a way that breaks typer < 0.15, and an
# unpinned transitive dependency defeats the point of this file. The image
# built fine and then died at startup on
# 'Secondary flag is not valid for non-boolean flag'.
click==8.5.0
PyYAML==6.0.2
