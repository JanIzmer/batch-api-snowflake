# batch-api-snowflake

Batch pipeline: public weather API -> landing zone -> Snowflake -> dbt (staging / marts),
orchestrated with Airflow, runnable locally through docker-compose.

Work in progress.
