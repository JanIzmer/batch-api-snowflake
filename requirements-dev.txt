-r requirements.txt
pytest==8.3.3
pytest-cov==5.0.0
respx==0.21.1
ruff==0.6.9
mypy==1.11.2
# Stub packages mypy needs. They live here rather than only in the
# pyproject dev extra because CI installs this file, and the two lists
# had silently drifted apart.
types-PyYAML==6.0.12.20240917
sqlfluff==3.1.1
sqlfluff-templater-dbt==3.1.1
